project  =terminal
main=src${~/~}rust${~/~}${project}
common =${projects}simscript${~/~}comm-build.7b:file
projects=..${~/~}
crate_dir=${projects}${~/~}crates

comp opts=[]

version="1.15:032"
set_env(VERSION,version)

mode=mode:prop
if {
	eq(mode,release) then {
		array(-C,opt-level=3)
		array(comp opts, ~~)
		assign(comp opts, ~~)
	}
}

include(common);